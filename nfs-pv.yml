apiversion: v1
kind: PersistentVolume
metadata:
    name: pv1000
spec:
    capacity:
        storage: 900Gi
    accessModes:
    - ReadWriteMany
    nfs:
        path: /export
        server: 192.168.42.1
    persistentVolumeReclaimPolicy: Recycle
